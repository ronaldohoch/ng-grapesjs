import { PipeObjectKeysPipe } from './pipe-object-keys.pipe';

describe('PipeObjectKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeObjectKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
